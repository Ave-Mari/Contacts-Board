import { createSlice } from '@reduxjs/toolkit';

const contactsSlice({
    name: 'contacts',
    initialState: {
        contacts: []
    },
    reducers: 
    {
        addContact() {},
        removeContact() {}
    }
})