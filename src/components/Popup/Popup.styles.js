import styled from 'styled-components';

export const Block = styled.div`
    position: absolute;
    top: 15%;
    width: 391px;
    height: 392px;  
    color: #fff;
    border-radius: 26px;
    //background: ${props => props.view === "create-contact" ? 'red' : 'blue'};

    .close-btn {
        position: absolute;
        background: none;
        border: none;
        font-size: 30px;
        color: #fff;
        right: 4%;
        top: 2%;
      }
    }

   

`